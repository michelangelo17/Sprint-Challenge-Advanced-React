import { Button } from "../emotionalThings/EmoTools";
import styled from '@emotion/styled/macro'

export const DarkModeButton = styled(Button)`
  position: absolute;
  top: 0;
  left: 0;
  padding: 20px;
`